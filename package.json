{
  "name": "questdex-coach",
  "version": "0.1.0",
  "private": true,
  "description": "A static QuestDex coach MVP with profile, quest, side-quest, and travel-radar generation.",
  "scripts": {
    "check": "node --check app.js",
    "test": "node --test",
    "start": "python3 -m http.server 8765"
  }
}
